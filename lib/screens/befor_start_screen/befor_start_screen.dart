import 'package:flutter/material.dart';

class BeforStartScreen extends StatelessWidget {
  const BeforStartScreen({super.key});
  static String routeName = 'BeforStartScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
